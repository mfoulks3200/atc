# @atc/docs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a Docusaurus v3 documentation site as `@atc/docs` with ATC/aviation-themed branding, serving existing repo docs.

**Architecture:** New `packages/docs/` directory using Docusaurus v3 classic preset with TypeScript config. Custom CSS variables for aviation dark theme. Existing markdown docs copied in with Docusaurus frontmatter added. Custom React landing page.

**Tech Stack:** Docusaurus 3.x, React 19, TypeScript 5.x, CSS custom properties

---

### Task 1: Scaffold Package and Install Dependencies

**Files:**
- Create: `packages/docs/package.json`
- Modify: `pnpm-workspace.yaml` (no change needed — already globs `packages/*`)

- [ ] **Step 1: Create package.json**

Create `packages/docs/package.json`:

```json
{
  "name": "@atc/docs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "docusaurus start",
    "build": "docusaurus build",
    "serve": "docusaurus serve",
    "clear": "docusaurus clear"
  },
  "dependencies": {
    "@docusaurus/core": "^3.9.2",
    "@docusaurus/preset-classic": "^3.9.2",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.1.1",
    "prism-react-renderer": "^2.3.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.9.2",
    "@docusaurus/tsconfig": "^3.9.2",
    "@docusaurus/types": "^3.9.2",
    "typescript": "^5.8.2"
  },
  "browserslist": {
    "production": [">0.5%", "not dead", "not op_mini all"],
    "development": ["last 3 chrome version", "last 3 firefox version", "last 5 safari version"]
  },
  "engines": {
    "node": ">=18.0"
  }
}
```

- [ ] **Step 2: Install dependencies**

Run: `pnpm install`

Expected: Dependencies install successfully. `pnpm-lock.yaml` updated.

- [ ] **Step 3: Commit**

```bash
git add packages/docs/package.json pnpm-lock.yaml
git commit -m "feat(docs): scaffold @atc/docs package with dependencies"
```

---

### Task 2: Docusaurus Configuration

**Files:**
- Create: `packages/docs/docusaurus.config.ts`
- Create: `packages/docs/sidebars.ts`
- Create: `packages/docs/tsconfig.json`

- [ ] **Step 1: Create tsconfig.json**

Create `packages/docs/tsconfig.json`:

```json
{
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "baseUrl": "."
  }
}
```

- [ ] **Step 2: Create docusaurus.config.ts**

Create `packages/docs/docusaurus.config.ts`:

```typescript
import { themes as prismThemes } from "prism-react-renderer";
import type { Config } from "@docusaurus/types";
import type * as Preset from "@docusaurus/preset-classic";

const config: Config = {
  title: "ATC Docs",
  tagline: "Agent orchestration with aviation precision",
  favicon: "img/favicon.ico",

  url: "https://atc-docs.example.com",
  baseUrl: "/",

  onBrokenLinks: "throw",
  onBrokenMarkdownLinks: "warn",

  i18n: {
    defaultLocale: "en",
    locales: ["en"],
  },

  presets: [
    [
      "classic",
      {
        docs: {
          sidebarPath: "./sidebars.ts",
          routeBasePath: "docs",
        },
        blog: false,
        theme: {
          customCss: "./src/css/custom.css",
        },
      } satisfies Preset.Options,
    ],
  ],

  themeConfig: {
    colorMode: {
      defaultMode: "dark",
      disableSwitch: false,
      respectPrefersColorScheme: false,
    },
    navbar: {
      title: "ATC Docs",
      items: [
        {
          type: "docSidebar",
          sidebarId: "docsSidebar",
          position: "left",
          label: "Documentation",
        },
        {
          href: "https://github.com/your-org/atc",
          label: "GitHub",
          position: "right",
        },
      ],
    },
    footer: {
      style: "dark",
      links: [
        {
          title: "Docs",
          items: [
            { label: "Specification", to: "/docs/specification" },
            { label: "Contributing", to: "/docs/contributing" },
            { label: "Agent Guide", to: "/docs/agent/operating-manual" },
          ],
        },
      ],
      copyright: `ATC — Air Traffic Control for Agents`,
    },
    prism: {
      theme: prismThemes.github,
      darkTheme: prismThemes.dracula,
    },
  } satisfies Preset.ThemeConfig,
};

export default config;
```

- [ ] **Step 3: Create sidebars.ts**

Create `packages/docs/sidebars.ts`:

```typescript
import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const sidebars: SidebarsConfig = {
  docsSidebar: [
    {
      type: "category",
      label: "Getting Started",
      items: ["introduction"],
    },
    {
      type: "category",
      label: "Reference",
      items: ["specification", "design-brief"],
    },
    {
      type: "category",
      label: "Guides",
      items: ["contributing"],
    },
    {
      type: "category",
      label: "Agent",
      items: ["agent/operating-manual"],
    },
  ],
};

export default sidebars;
```

- [ ] **Step 4: Commit**

```bash
git add packages/docs/tsconfig.json packages/docs/docusaurus.config.ts packages/docs/sidebars.ts
git commit -m "feat(docs): add Docusaurus config, sidebars, and tsconfig"
```

---

### Task 3: Aviation Theme CSS

**Files:**
- Create: `packages/docs/src/css/custom.css`

- [ ] **Step 1: Create custom.css**

Create `packages/docs/src/css/custom.css`:

```css
/* ATC Aviation Theme */

:root {
  /* Light mode — kept functional but styled to match brand */
  --ifm-color-primary: #16213e;
  --ifm-color-primary-dark: #121c34;
  --ifm-color-primary-darker: #101a31;
  --ifm-color-primary-darkest: #0d1528;
  --ifm-color-primary-light: #1a2648;
  --ifm-color-primary-lighter: #1c284b;
  --ifm-color-primary-lightest: #213054;
  --ifm-font-family-base: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
    Oxygen, Ubuntu, sans-serif;
  --ifm-heading-font-family: "SF Mono", "Cascadia Code", "Fira Code", Menlo,
    Consolas, monospace;
  --ifm-code-font-size: 95%;
  --docusaurus-highlighted-code-line-bg: rgba(0, 255, 65, 0.1);
}

[data-theme="dark"] {
  --ifm-color-primary: #00ff41;
  --ifm-color-primary-dark: #00e63b;
  --ifm-color-primary-darker: #00d938;
  --ifm-color-primary-darkest: #00b32e;
  --ifm-color-primary-light: #1aff54;
  --ifm-color-primary-lighter: #26ff5a;
  --ifm-color-primary-lightest: #4dff73;
  --ifm-background-color: #1a1a2e;
  --ifm-background-surface-color: #16213e;
  --ifm-navbar-background-color: #0f0f1e;
  --ifm-footer-background-color: #0a0a16;
  --ifm-font-color-base: #e0e0e0;
  --ifm-heading-color: #ffffff;
  --ifm-link-color: #00ff41;
  --ifm-link-hover-color: #39ff14;
  --ifm-menu-color-active: #00ff41;
  --ifm-menu-color-background-active: rgba(0, 255, 65, 0.1);
  --ifm-toc-link-color: #a0a0a0;
  --ifm-code-background: rgba(0, 255, 65, 0.08);
  --ifm-code-color: #00ff41;
  --docusaurus-highlighted-code-line-bg: rgba(0, 255, 65, 0.15);
}

/* Monospace headings for instrument panel feel */
h1,
h2,
h3,
h4,
h5,
h6 {
  font-family: var(--ifm-heading-font-family);
  letter-spacing: -0.02em;
}

/* Navbar styling */
.navbar {
  border-bottom: 1px solid rgba(0, 255, 65, 0.15);
}

[data-theme="dark"] .navbar {
  box-shadow: 0 1px 0 0 rgba(0, 255, 65, 0.1);
}

/* Sidebar active item glow */
[data-theme="dark"] .menu__link--active {
  border-left: 2px solid #00ff41;
  padding-left: calc(var(--ifm-menu-link-padding-horizontal) - 2px);
}

/* Admonition / callout amber accent */
[data-theme="dark"] .alert--warning {
  --ifm-alert-background-color: rgba(255, 191, 0, 0.1);
  --ifm-alert-border-color: #ffbf00;
  --ifm-alert-foreground-color: #ffbf00;
}

/* Footer */
.footer {
  border-top: 1px solid rgba(0, 255, 65, 0.15);
}

/* Table styling */
[data-theme="dark"] table {
  border-color: rgba(0, 255, 65, 0.15);
}

[data-theme="dark"] table thead {
  background-color: rgba(0, 255, 65, 0.05);
}

[data-theme="dark"] table tr:nth-child(2n) {
  background-color: rgba(0, 255, 65, 0.03);
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/docs/src/css/custom.css
git commit -m "feat(docs): add aviation-themed dark CSS"
```

---

### Task 4: Copy and Prepare Existing Docs

**Files:**
- Create: `packages/docs/docs/introduction.md`
- Create: `packages/docs/docs/specification.md`
- Create: `packages/docs/docs/contributing.md`
- Create: `packages/docs/docs/design-brief.md`
- Create: `packages/docs/docs/agent/operating-manual.md`

- [ ] **Step 1: Create introduction.md**

Create `packages/docs/docs/introduction.md`:

```markdown
---
title: Introduction
sidebar_position: 1
slug: /
---

# ATC — Air Traffic Control

ATC is an agent orchestration system that coordinates multiple autonomous agents working on concurrent code changes in a shared repository. It uses aviation terminology as its domain language — changes are "crafts" flown by "pilots" who navigate "vectors" and request "landing clearance" from a "tower" to merge.

## Quick Reference

| Term | Meaning |
|---|---|
| Craft | Unit of work tied to a git branch |
| Pilot | Autonomous agent with certifications |
| Captain | Pilot-in-command, final authority |
| First Officer | Certified co-pilot, can modify code |
| Jumpseat | Observer/advisor, cannot modify code |
| Vector | Milestone with acceptance criteria |
| Flight Plan | Ordered sequence of vectors |
| Black Box | Append-only event log on every craft |
| Tower | Merge coordinator, one per repo |
| Controls | Exclusive or shared code modification rights |

## Documentation

- **[Specification](/docs/specification)** — The authoritative formal spec with numbered `RULE-*` identifiers
- **[Contributing](/docs/contributing)** — Validation checklist for all changes
- **[Design Brief](/docs/design-brief)** — The original informal design notes
- **[Agent Operating Manual](/docs/agent/operating-manual)** — Behavioral guidance for agents operating as pilots
```

- [ ] **Step 2: Copy specification.md with frontmatter**

Read the full `docs/specification.md` from the repo root. Create `packages/docs/docs/specification.md` by prepending this frontmatter:

```markdown
---
title: Formal Specification
sidebar_position: 2
---
```

Then append the full content of the original `docs/specification.md` below the frontmatter.

- [ ] **Step 3: Copy contributing.md with frontmatter**

Read `docs/contributing.md`. Create `packages/docs/docs/contributing.md` by prepending:

```markdown
---
title: Contributing
sidebar_position: 3
---
```

Then append the full content of the original `docs/contributing.md`.

- [ ] **Step 4: Copy spec.md as design-brief.md with frontmatter**

Read `docs/spec.md`. Create `packages/docs/docs/design-brief.md` by prepending:

```markdown
---
title: Design Brief
sidebar_position: 4
---
```

Then append the full content of the original `docs/spec.md`.

- [ ] **Step 5: Copy operating-manual.md with frontmatter**

Create directory `packages/docs/docs/agent/`. Read `docs/agent/operating-manual.md`. Create `packages/docs/docs/agent/operating-manual.md` by prepending:

```markdown
---
title: Agent Operating Manual
sidebar_position: 1
---
```

Then append the full content of the original `docs/agent/operating-manual.md`.

- [ ] **Step 6: Commit**

```bash
git add packages/docs/docs/
git commit -m "feat(docs): add existing docs with Docusaurus frontmatter"
```

---

### Task 5: Custom Landing Page

**Files:**
- Create: `packages/docs/src/pages/index.tsx`
- Create: `packages/docs/src/pages/index.module.css`

- [ ] **Step 1: Create index.module.css**

Create `packages/docs/src/pages/index.module.css`:

```css
.hero {
  padding: 4rem 2rem;
  text-align: center;
  position: relative;
  overflow: hidden;
  min-height: 60vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

[data-theme="dark"] .hero {
  background: radial-gradient(
    ellipse at center,
    rgba(0, 255, 65, 0.05) 0%,
    transparent 70%
  );
}

.heroInner {
  max-width: 800px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}

.title {
  font-size: 3.5rem;
  font-family: "SF Mono", "Cascadia Code", "Fira Code", Menlo, Consolas,
    monospace;
  letter-spacing: -0.03em;
  margin-bottom: 0.5rem;
}

[data-theme="dark"] .title {
  color: #ffffff;
}

.subtitle {
  font-size: 1.5rem;
  margin-bottom: 2rem;
  opacity: 0.8;
}

[data-theme="dark"] .subtitle {
  color: #a0a0a0;
}

.tagline {
  font-family: "SF Mono", "Cascadia Code", "Fira Code", Menlo, Consolas,
    monospace;
  font-size: 0.9rem;
  margin-bottom: 2.5rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

[data-theme="dark"] .tagline {
  color: #00ff41;
}

.buttons {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.primaryButton {
  padding: 0.75rem 2rem;
  border-radius: 4px;
  font-weight: 600;
  font-size: 1rem;
  text-decoration: none;
  transition: all 0.2s ease;
}

[data-theme="dark"] .primaryButton {
  background: #00ff41;
  color: #0a0a16;
  border: 1px solid #00ff41;
}

[data-theme="dark"] .primaryButton:hover {
  background: #39ff14;
  color: #0a0a16;
  text-decoration: none;
}

.secondaryButton {
  padding: 0.75rem 2rem;
  border-radius: 4px;
  font-weight: 600;
  font-size: 1rem;
  text-decoration: none;
  transition: all 0.2s ease;
}

[data-theme="dark"] .secondaryButton {
  background: transparent;
  color: #00ff41;
  border: 1px solid rgba(0, 255, 65, 0.4);
}

[data-theme="dark"] .secondaryButton:hover {
  border-color: #00ff41;
  text-decoration: none;
}

.features {
  padding: 4rem 2rem;
  max-width: 1000px;
  margin: 0 auto;
}

.featureGrid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
}

.featureCard {
  padding: 1.5rem;
  border-radius: 8px;
  transition: transform 0.2s ease;
}

.featureCard:hover {
  transform: translateY(-2px);
}

[data-theme="dark"] .featureCard {
  background: rgba(22, 33, 62, 0.6);
  border: 1px solid rgba(0, 255, 65, 0.1);
}

.featureIcon {
  font-size: 2rem;
  margin-bottom: 0.75rem;
}

.featureTitle {
  font-family: "SF Mono", "Cascadia Code", "Fira Code", Menlo, Consolas,
    monospace;
  font-size: 1.1rem;
  margin-bottom: 0.5rem;
}

[data-theme="dark"] .featureTitle {
  color: #ffffff;
}

.featureDescription {
  font-size: 0.95rem;
  line-height: 1.5;
}

[data-theme="dark"] .featureDescription {
  color: #a0a0a0;
}

/* Radar sweep animation */
.radarSweep {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 400px;
  height: 400px;
  margin-top: -200px;
  margin-left: -200px;
  border-radius: 50%;
  opacity: 0.15;
  pointer-events: none;
}

[data-theme="dark"] .radarSweep {
  border: 1px solid rgba(0, 255, 65, 0.2);
  background: conic-gradient(
    from 0deg,
    transparent 0deg,
    rgba(0, 255, 65, 0.1) 30deg,
    transparent 60deg
  );
  animation: sweep 4s linear infinite;
}

@keyframes sweep {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
```

- [ ] **Step 2: Create index.tsx**

Create `packages/docs/src/pages/index.tsx`:

```tsx
import Link from "@docusaurus/Link";
import Layout from "@theme/Layout";
import styles from "./index.module.css";

const features = [
  {
    icon: "✈️",
    title: "Craft Management",
    description:
      "Every change is a craft with a callsign, cargo, and flight plan. Full lifecycle from filing to landing.",
  },
  {
    icon: "🗼",
    title: "Tower Coordination",
    description:
      "A single tower per repo manages landing clearance, conflict detection, and merge sequencing.",
  },
  {
    icon: "📦",
    title: "Black Box Logging",
    description:
      "Append-only event logs on every craft capture decisions, state transitions, and audit trails.",
  },
];

export default function Home(): React.JSX.Element {
  return (
    <Layout description="Agent orchestration with aviation precision">
      <header className={styles.hero}>
        <div className={styles.radarSweep} />
        <div className={styles.heroInner}>
          <h1 className={styles.title}>ATC</h1>
          <p className={styles.subtitle}>Air Traffic Control for Agents</p>
          <p className={styles.tagline}>
            Coordinate autonomous agents with aviation precision
          </p>
          <div className={styles.buttons}>
            <Link className={styles.primaryButton} to="/docs">
              Read the Docs
            </Link>
            <Link
              className={styles.secondaryButton}
              to="/docs/specification"
            >
              View Specification
            </Link>
          </div>
        </div>
      </header>
      <section className={styles.features}>
        <div className={styles.featureGrid}>
          {features.map((feature) => (
            <div key={feature.title} className={styles.featureCard}>
              <div className={styles.featureIcon}>{feature.icon}</div>
              <h3 className={styles.featureTitle}>{feature.title}</h3>
              <p className={styles.featureDescription}>
                {feature.description}
              </p>
            </div>
          ))}
        </div>
      </section>
    </Layout>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/docs/src/pages/
git commit -m "feat(docs): add custom landing page with radar animation"
```

---

### Task 6: Static Assets

**Files:**
- Create: `packages/docs/static/.nojekyll`

- [ ] **Step 1: Create .nojekyll**

Create `packages/docs/static/.nojekyll` (empty file, needed for GitHub Pages if deployed there):

```bash
touch packages/docs/static/.nojekyll
```

- [ ] **Step 2: Create static/img directory**

```bash
mkdir -p packages/docs/static/img
```

Create a placeholder `packages/docs/static/img/.gitkeep` (empty file so git tracks the directory).

```bash
touch packages/docs/static/img/.gitkeep
```

- [ ] **Step 3: Commit**

```bash
git add packages/docs/static/
git commit -m "feat(docs): add static assets directory"
```

---

### Task 7: Root Integration and Verification

**Files:**
- Modify: `package.json` (root)

- [ ] **Step 1: Add docs scripts to root package.json**

Add these scripts to the root `package.json` `scripts` object:

```json
"docs:dev": "pnpm --filter @atc/docs dev",
"docs:build": "pnpm --filter @atc/docs build"
```

- [ ] **Step 2: Verify the site starts**

Run from repo root:

```bash
pnpm docs:dev -- --no-open
```

Expected: Docusaurus dev server starts successfully on port 3000. Press Ctrl+C after confirming it starts.

- [ ] **Step 3: Verify the site builds**

Run:

```bash
pnpm docs:build
```

Expected: Static site builds successfully into `packages/docs/build/`.

- [ ] **Step 4: Add build output to .gitignore**

If `packages/docs/.gitignore` does not exist, create it:

```
build/
.docusaurus/
node_modules/
```

- [ ] **Step 5: Commit**

```bash
git add package.json packages/docs/.gitignore
git commit -m "feat(docs): add root scripts and gitignore for docs package"
```
